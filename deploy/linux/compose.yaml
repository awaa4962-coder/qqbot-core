name: qqfriend

services:
  napcat:
    image: ${NAPCAT_IMAGE:-mlikiowa/napcat-docker:v4.18.13}
    container_name: qqfriend-napcat
    restart: unless-stopped
    environment:
      ACCOUNT: ${NAPCAT_ACCOUNT:-}
      NAPCAT_UID: ${NAPCAT_UID:-1000}
      NAPCAT_GID: ${NAPCAT_GID:-1000}
    volumes:
      - ./state/napcat/config:/app/napcat/config
      - ./state/napcat/qq:/app/.config/QQ
    ports:
      - "127.0.0.1:6099:6099"
      - "127.0.0.1:6700:6700"
      - "127.0.0.1:3001:3001"
    security_opt:
      - no-new-privileges:true

  bridge:
    build:
      context: ../..
      dockerfile: deploy/linux/Dockerfile
      args:
        PIP_INDEX_URL: ${PIP_INDEX_URL:-https://pypi.org/simple}
    image: qqfriend-bridge:${QQFRIEND_IMAGE_TAG:-linux-preview}
    container_name: qqfriend-bridge
    restart: unless-stopped
    depends_on:
      - napcat
    env_file:
      - path: ./qqfriend.env
        required: true
    environment:
      QQFRIEND_CONTAINERIZED: "1"
      QQBOT_LISTEN_HOST: 0.0.0.0
      QQBOT_NAPCAT_API: http://napcat:6700
      QQBOT_NAPCAT_WS_API: ws://napcat:3001
      QQBOT_CONFIG_ROOT: /config
      QQBOT_DATA_DIR: /data
      QQBOT_LOG_DIR: /logs
      QQBOT_TEMP_DIR: /tmp/qqfriend
      TMPDIR: /tmp/qqfriend
      QQBOT_JM_PYTHON: python
    volumes:
      - ./state/qqfriend/config:/config
      - ./state/qqfriend/data:/data
      - ./state/qqfriend/logs:/logs
      - ./state/qqfriend/temp:/tmp/qqfriend
    ports:
      - "127.0.0.1:16789:16789"
    read_only: true
    tmpfs:
      - /tmp:size=256m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
