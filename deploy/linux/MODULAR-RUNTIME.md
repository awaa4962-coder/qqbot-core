# QQFriend Linux 模块边界与目标实现

目标规划更新：2026-09-23。总顺序和阶段状态见 [智能化与模块化总路线](ROADMAP.md)。本文先保留已交付的维护约定，再列出尚未实现的目标、代码入口、任务和验收。

历史维护基线：`1.4.7-modular-runtime`。下述历史整理以职责和重复流程为边界，不更换前端框架、不引入数据库，也不改变聊天模型路由、关系评分和用户白名单。新目标以 `1.4.12-model-refresh` 源码为核对基线，Windows 安装继续冻结。

## 当前边界

| 位置 | 职责 | 不负责 |
| --- | --- | --- |
| `Web/app.js` | 导航、页面事件与初始化 | 各功能表单和列表的实现 |
| `Web/pages/` | API、梗库、表情、配置等页面渲染 | 直接写服务器文件 |
| `Web/ui/` | 明确的共享状态、操作反馈、轮询和宿主适配 | 推测任务已取消、擅自重新提交 |
| `bridge/tasks/runner.mjs` | 任务生命周期、并发、进度和重启中断状态 | 业务权限、QQ 发送重试、下载调度规则 |
| `bridge/admin-api/task-manager.mjs` | 固定操作白名单和公开结果裁剪 | 客户端指定文件路径、执行任意命令 |
| `bridge/jm/` | 命令、运行检查、压缩、临时目录和共用传输流程 | 合并群白名单与私聊白名单 |
| `bridge/persistence/json-file.mjs` | 原子写入、保存排队和退出 flush | 画像算法、数据迁移、跨文件事务 |
| `bridge/memory-profile/` | 存储、事件更新、查询和展示 | 改变已有画像规则与关系分数 |
| `group-summary/generation-plans.mjs` | 结构化与旧格式展示策略 | 重复实现主备模型执行 |

`jm-provider.mjs` 和 `memory-profile.mjs` 是兼容入口，原导出仍可用。旧 LLM 客户端保留调用形状，共用 `api-providers/transport.mjs`；一次旧请求仍只尝试一次。正式聊天任务继续使用已有任务路由，不改主备分配。

## 后台任务

- 浏览器耗时操作先获得任务编号，再查询进度。旧 `/admin/memes`、`/admin/stickers`、`/admin/diagnose/replay` 接口不删除，现有调用方仍可使用。
- 日报沿用自己的 `summaries/jobs.json`；其他管理任务元数据位于私有数据目录 `.qqfriend/tasks/admin.json`。任务输入和结果正文不落任务账本。
- 同一类管理任务互斥，最多两个并行；日报仍按群和日期互斥并限制两个并行。模块自身的白名单、业务锁和用量限制不变。
- 默认五分钟标记等待过久。AbortSignal 是中止请求，不等于工作已经停止；底层操作未结束时继续占用任务范围。没有自动重试、自动续跑或假完成。
- 重启后未完成任务标记中断，管理员需查看对应模块状态。日报发送是否完成以发送账本为准，不能清空账本后重试。
- 完成结果在内存中最多保留十分钟、最多二十份。已持久化的词库、表情与回放可以刷新读取；未保存的查证结果过期后需要重新查证，不能宣称永久保存。

## 维护与验证

新增页面使用 ES 模块并显式导入依赖，同时登记 `web-console.mjs` 的静态资源白名单。不要把共享状态命名成业务函数常用的局部参数名；页面状态采用 `uiState`。

新增耗时管理动作要同时声明允许的模块/动作与输入字段，保留业务操作自身的权限和幂等约束。不要把业务失败包装成任务成功。结果返回前移除原始接口响应、凭据和无关的完整目录快照。

存档测试必须覆盖保存失败、写入期间新修改、退出同步保存与旧异步快照竞争。公共 JSON 工具不提供跨进程锁或多文件事务，日报仍使用自身写锁，其他模块不要假定它能替代业务锁。

发布门禁为 `npm ci`、`npm run lint`、`npm test`、`npm run replay:check` 与 `npm run release:check`。前端 JS 已纳入 ESLint，测试会验证 ES 模块链接和每个导入资源的可访问性；还应使用浏览器检查实际页面操作、长任务刷新恢复及手机宽度。测试数量通过不代表真实模型的答案质量已经验证。

## 目标实现说明

本节区分已完成的 P0、实施中的 P1 与尚未实施的 P2-P5，不表示所有拟议接口已上线。文件路径是已核对的现有入口，不要求逐项新增同名模块或一次性移动文件。实施前重新核对调用方、线上配置和最新工作区；只改当前批次。

### A. 目标模块边界

| 模块 | 现有入口 | 目标职责 | 禁止越界 |
| --- | --- | --- | --- |
| 接入与发送 | `bridge/onebot-link.mjs`、`bridge/napcat.mjs` | 标准化事件、连接生命周期、确认外发结果 | 组织提示词、推测模型意图 |
| 权限与能力 | `bridge/commands/permissions.mjs`、`bridge/commands/manifest.mjs`、`bridge/capabilities/catalog.mjs` | 权限判断、能力状态和命令声明 | 自动开放管理员动作给模型 |
| 对话调度 | `bridge/reply.mjs`、`reply-group.mjs`、`reply-private.mjs`、`reply-ai.mjs` | 分流、对话生命周期、等待与执行协调 | 重复维护画像、模型协议和存储 |
| 上下文与记忆 | `bridge/context/`、`context-retriever.mjs`、`memory-profile/` | 选择相关证据、管理记忆、预算与失效 | 绕过范围读取其他群/私聊 |
| 提示词 | `bridge/system-prompts/`、`bridge/persona-style.mjs` | 确定性组装规则、任务模板和动态资料 | 自行读取存储、写状态、鉴权 |
| 模型与工具 | `bridge/model-router.mjs`、`model-mimo.mjs`、`model-ds.mjs`、`api-providers/` | 协议适配、预算、工具执行与受控降级 | 自行决定群权限或收件人 |
| 业务能力 | `bridge/group-summary/`、`features/conversation-summary/`、`features/stickers/`、`jm/` | 各自业务规则、事实校验和任务幂等 | 各写一套模型运输、通用鉴权 |
| 观测与管理 | `bridge/diagnostics/`、`api-providers/usage-metrics.mjs`、`admin-api/`、`Web/pages/` | 追踪、用量、受保护的查询和编辑 | 默认落完整聊天、提示词或私有推理 |

依赖方向是“管理/消息入口 -> 业务协调 -> 能力与基础设施”。业务模块通过明确入口协作，调度层只决定调用顺序，不吞并各模块的状态机。共享原子写入工具不能代替业务锁或跨文件事务。

### B. 最小交互契约

以下是拟议的数据含义，不是已导出的 API。优先给现有接口加适配与明确字段，不引入全局总线或新的通用框架。

| 契约 | 最少信息 | 约束 |
| --- | --- | --- |
| 请求范围 | 消息 ID、真实发送人、群/私聊范围、引用/@对象、权限、隐私代次 | 由后端生成，模型不得更改范围 |
| 对话快照 | 对话编号、修订号、当前问题、参与者、来源消息、待确认事项 | 昵称不代替身份；旧修订结果不能覆盖已纠正的新状态 |
| 能力快照 | 功能 ID、支持范围、启用/许可/健康状态、版本与观察时间 | 未知不等于正常；受权过滤，不返回密钥和路径 |
| 记忆条目 | 类型、来源 ID、所属范围、时间、状态、到期/替代关系、必要可信度 | 明确偏好优先；无来源的旧推测不能变成事实 |
| 模型结果 | `reply` / `silence` / `needs_input` / `error`、正文、原因码、实际模型和用量 | 工具协议及中间结果内部处理，不把任意文本当动作 |
| 发送结果 | `confirmed` / `failed` / `unknown`、已确认分段与消息 ID | `unknown` 不自动重发；生成成功不等于发送成功 |

长任务继续使用现有任务状态，不与模型结果枚举合成巨型状态机。任务执行成功、内容生成成功和消息送达成功分别表达。

### C. P0 基线与退路

目标：先获得可比较、可恢复的基线。规划文件完成不等于 P0 完成。

- [x] P0-01 核对本地提交、Linux 发布提交、容器镜像、模型任务路由和实际启用模块；只记录脱敏指纹及必要元数据。
- [x] P0-02 保存源码安全备份，以及服务器访问受限的配置/记忆/任务账本备份；明确恢复顺序和备份权限。
- [x] P0-03 运行既有门禁，记录通过、失败、跳过及平台原因，不借删除测试降低失败数。
- [x] P0-04 建立固定案例与评分表；记录模型、任务、思考档位、提示词指纹和调用预算，不保存真实私密对话到仓库。
- [x] P0-05 清点梗库调用、现有能力注册、上下文入口、模型回退与发送调用方；记录保留、迁移、退役清单。

验收：能够从基线复现案例，备份可读，恢复方法明确；离线回放与实际模型评价分开记录。首次实施若 SSH 不通，允许推进本地分析，但不得宣称服务器备份和核验完成。

#### P0 实际记录（2026-09-23）

- 核对基线代码 `1c4ab31` / `1.4.12-model-refresh` 与生产一致，SSH、健康、就绪和 QQ 登录状态正常。源码归档 SHA-256 为 `b0f6d53c10ef45769ac4a94f34f103baf0557455e206809d9c32b829e11fc1d8`。
- 服务器备份目录权限 0700、归档 0600；Bridge 正常退出落盘后备份配置、数据和日报发送/补发账本。同版 Bridge 重新启动并就绪，NapCat 没有停止。归档解压到隔离目录，138 个 JSON 文件可解析；没有向生产回灌旧数据。
- 本地 `npm ci` 与 lint 通过，877 项测试中 876 通过、1 项 Windows 软链接权限跳过。Windows `release:check` 因未装 `jmcomic` 不通过；不为此更新冻结的 Windows 安装。
- 使用相同源码的隔离 Linux 容器补验：877/877 测试、13/13 离线回放、lint、依赖检查、隔离启动和 `release:check` 通过；隔离启动没有真实 OneBot，`not_ready` 是预期。生产实际 `/ready` 正常，JM 实际依赖检查通过，7-Zip 可用且密码仍为大写 `FS`。
- 固定全部 11 个合成案例的输入指纹与期望，并以其中 3 个样例建立首次真实回答基线。最多允许 6 次请求，实际 3 次，均由当前群聊主模型 `mimo-v2.6-flash` 按既有 `deep` 档位完成，未触发备用；没有读取真实群记录或发送 QQ 消息。
- 实际合成调用合计输入 1463 Token，其中接口报告缓存命中 768；输出 587 Token，其中报告思考 215。它们是三个小样例的观测，不是稳定命中率、完整聊天成本或质量保证。只保存清洗后的最终正文与元数据，不保存私有推理。
- 备份恢复说明、模型/路由快照、完整合成回答与原始验证日志仅保存在受限运维记录中，不进入公开仓库。若需要回退，先回退代码与镜像，不能直接覆盖上线后的聊天和发送账本。

| 固定案例 | 输入指纹 | 基线评价 | 后续比较要求 |
| --- | --- | --- | --- |
| `continuation` 短句承接 | `bbfc6c2d82a5d93d` | 未达标：重复已建议的密码检查，猜测无依据的密码变体，回答过长 | 承接已尝试步骤；先给有依据的下一步或追问，不编密码 |
| `meme-context` 表情语境 | `b00839bc2a0e44e6` | 本例符合预期：结合临时加班理解反讽 | 保持语境理解；此例使用文字画面描述，不等同真实图像识别验收 |
| `unknown-meme` 未知梗 | `2877fcf503efc046` | 本例符合预期：说明不认识，询问出处，没有编起源 | 保持不编造；未来有受限检索时按需查证 |

基线出现问题不妨碍 P0 完成；P0 的目的正是把问题记录为后续可比较目标。一次模型输出不代表稳定成功或稳定失败。

| 清点对象 | 处理方向 | 迁移注意点 |
| --- | --- | --- |
| `startup.mjs`、`reply-group.mjs`、`context-retriever.mjs` 的梗库启动/学习/注入 | 退役活跃调用 | 定时更新、启动衰减和消息学习都要停止，不只隐藏前端 |
| `commands/modules/basic.mjs`、`admin.mjs`、`admin-api/meme-manager.mjs`、`task-manager.mjs` 与前端梗页 | 停用兼容与后续清理 | 旧请求不能通过任务入口重新启动联网更新 |
| `knowledge/memes/image-context.mjs` | 保留视觉能力并另行理顺归属 | 视觉、表情分类和分析共享该文件，不能整目录删除 |
| `capabilities/catalog.mjs`、`commands/manifest.mjs`、`self-description.mjs` | 统一事实来源与权限投影 | 已有能力目录和命令注册，不再新增平行清单 |
| `model-router.mjs`、`reply-ai.mjs`、`reply-private.mjs` | 迁移明确结果契约 | 保留现有主备与发送确认，区分主动沉默和失败 |
| `jm/`、日报、成员总结、表情、关系、鉴权及 URL 安全 | 保留并回归 | JM 不是梗库；不改变独立白名单、FS 与延迟清理 |

### D. P1 能力事实与梗库退役

入口：`bridge/capabilities/catalog.mjs`、`commands/manifest.mjs`、`commands/permissions.mjs`、`self-description.mjs`、`knowledge/memes/`、`admin-api/` 和对应前端页面。

- [ ] P1-01 复用现有能力目录与命令声明，提取共享 ID 和权限投影，避免增加第三份功能清单。保留不以命令触发的能力。
- [ ] P1-02 从实际配置生成能力快照，区分已安装、已启用、当前调用者许可、服务健康与未知；后端执行时重新检查权限。
- [x] P1-03 分离开发者项目说明和聊天可见的自身信息。修正过期 Windows/诊断描述；实际模型身份由每次主/备调用解析后写入动态资料。
- [ ] P1-04 逐入口引入明确结果语义，先加兼容适配和契约测试，不一次改变所有调用者的返回类型。
- [x] P1-05 停止梗库学习、联网更新、定时任务和文本注入；清点图片语境中的依赖，保留通用图片理解与表情素材。
- [x] P1-05a 核验 JM 不受退役影响：群/私聊独立白名单、大写 `FS`、下载/压缩/跨容器上传/确认/延迟清理均保留；使用合成资源做离线回归，并检查 Linux 实际依赖，不自动发真实下载测试到 QQ。
- [x] P1-06 更新帮助、能力页和管理入口；旧梗库数据归档，旧接口返回明确停用状态，不触发模型请求或自动重新启用。
- [ ] P1-07 写明兼容适配器的调用方与删除条件，在后续批次移除死路径，不删除人工数据或为了退役删安全测试。

验收：普通用户看不到管理员专属细节；关闭模块后帮助/前端/自述同步变化，执行端也拒绝。模拟主备切换时模型身份正确。停用梗库后无采集、更新或注入副作用，表情、视觉、搜索、关系和日报回归通过。

前端：展示能力可用性及简短原因；保留受保护的归档说明，不把停用旧功能伪装成在线能力。回滚：恢复旧代码和兼容配置即可读取原有数据，不能同时运行新旧定时任务。

#### P1 首批：能力状态（1.4.13）

本批复用能力目录、命令权限和消息准入，提取 API 配置就绪检查供帮助与运行页共享；增加不混淆配置/权限/依赖的状态字段，并修正 Linux 开发者自描述。JM 仍是正式能力，使用已有运行检查缓存。

这不是 P1 全部完成：聊天模型尚未接入能力快照，明确模型结果契约和梗库退役仍待实施，P1 总任务不提前勾选。运行配置不变，未给模型新增工具权限。

验证记录：本地 lint 0 errors / 0 warnings，测试 888 通过、1 项 Windows 软链接权限跳过；相同代码的隔离 Linux 候选 889/889 测试、13/13 离线回放、启动鉴权冒烟、依赖及发布检查通过。候选挂载只读真实配置核验群/私聊/视觉路由、JM 依赖、管理员豁免边界和关系导出预留态，无模型调用和 QQ 发送。能力页使用合成配置与真实管理路由在 1440/390 像素浏览器验证搜索、筛选、状态和无横向溢出。新增 12 项测试，原测试未删除。

#### P1 第二批：自动梗库退役（1.4.14）

运行入口已移除学习、衰减、定时更新、提示词注入与退出存档；只读归档不依赖旧 store，不初始化种子或修改数据。旧管理写入返回 410，任务入口拒绝更新/查证，旧客户端动作和任务恢复不能重新提交或回填。前端入口位于维护页，不再是主导航功能。

保留 `knowledge/memes/image-context.mjs`，视觉与表情指纹仍依赖它；JM、搜索、链接预览不属于退役范围。测试覆盖入口依赖图、原文件不变、错误/大文件/软链接处理、权限、命令、旧任务和前端只读行为。旧业务测试按新退役语义更新，测试数未减少。

候选验收：本地 lint 零错误零告警、899 通过和 2 项 Windows 文件软链接权限跳过；Linux 901/901 测试、13/13 离线回放、隔离启动及发布检查通过。隔离候选只读读取真实旧词条，验证源文件不变；旧开启环境变量仍无法启用更新；实际 7-Zip 合成加密 ZIP 验证 FS 成功而 fs 失败。浏览器在 1440/390 像素验证维护入口、只读搜索、HTML 转义、旧请求 410 与无横向溢出，未启动 Windows Bot。

上线记录（2026-09-23）：代码提交 `e6ee0a6` 已推送并通过 GitHub CI，Linux 已切换到 `1.4.14-meme-retirement`。切换前保存受限私有状态备份并另存 0600 权限的旧词条原件；上线后原文件、API 配置和私有 sidecar 校验和均不变。实际 `/health`、`/ready` 与 QQ 登录正常，旧管理写入返回 410、后台更新拒绝、归档只读可访问，JM 实际依赖和私聊帮助通过；日报计划仍一份。NapCat 未重启，验证未调用模型或向 QQ 发送测试内容。

剩余兼容代码：`knowledge/memes/index.mjs` 及旧 store/schema/matcher/updater 等目前只供历史 helper 测试和旧导入兼容，不进入生产入口图；`meme-manager.mjs` 是只读查询/明确拒绝写入的薄适配，旧命令 ID 保留停用提示。P5 清理时核对外部已知调用方，将仍有价值的数据格式/隐私回归迁移后再移除死实现，不能为了删除代码删掉安全断言或重新接回旧管线。P1-07 尚未完成。

#### P1 第三批：会话自身信息与聊天结果（1.4.15）

`capabilities/self-context.mjs` 从能力目录生成会话内的短投影，API gateway 在实际模型解析后插入，最大 1800 字符、无时间戳，保留原系统前缀和工具往返。字段仅含身份/版本、请求模型、会话种类、允许能力的名称/状态/示例、真实工具名称和未启用功能；不含用户/群名单、端点、服务器路径、完整配置或私有推理。JM 只读已检查的依赖缓存，保留未知状态，不逐请求拉起 Python。

`chat-outcome.mjs` 定义 reply / silence / error。群/私聊模型路由只对 error 启用原有备用；严格空 reply 为主动沉默，格式错误或仅推理不是沉默。普通直接请求失败发送固定提示，但不创建成功轮次、表情或画像副作用；被动插话失败安静结束，不再随机应付。前端诊断显示主动不回复和安全事实元数据。

兼容入口 `tryMiMo`、`tryDeepSeek`、`parseMiMoResponse` 仍返回 text/null，既有搜索工具轮次、关系短评、摘要及历史测试继续使用；正式聊天路由使用新增 typed 入口。兼容函数不另选模型或另发消息。待全部实际调用方迁移至明确结果契约后才移除，不仅凭文件名判断死代码。

P1-03 已完成工程接入并上线；P1-04 已覆盖聊天入口，其他结果契约的完整一致性继续检查。P2-05 的权限/隐私代次复核在后续 1.4.16 批次接入并验证。动态事实预算独立于当前上下文预算，整体提示词版本、稳定前缀和缓存对照在 P2/P4 继续实现。

候选验收：`npm ci`、lint 0/0，本地 917 通过与 2 项 Windows 软链接权限跳过，Linux 919/919、13/13 离线回放、启动鉴权和发布检查全部通过。真实配置只读挂载核对主/备请求模型和权限投影，JM 实际依赖及 FS 加密包验证通过。实际群/私聊入口以合成模型与确认回执验证失败提示不写成功轮次、沉默只调用一次且不发送；浏览器 1440/390 像素验证筛选、元数据、无横向溢出。没有真实模型调用或 QQ 测试发送；上线状态另行记录。

上线记录（2026-09-23）：代码提交 `2868c14` 已推送并通过 GitHub CI，Linux 已切换 `1.4.15-self-context`，旧源码和镜像保留。切换前完成受限状态备份；上线后健康/就绪返回 200、QQ 已登录，JM 依赖、私聊帮助和新诊断资源通过。API 配置、私有 sidecar 与旧梗库原件校验和不变，日报计划仍一份。仅重启 Bridge，未重启 NapCat、未更新 Windows、未发测试 QQ 消息。

### E. P2 对话核心与提示词

入口：`bridge/reply*.mjs`、`bridge/context/`、`context-builder.mjs`、`system-prompts/`、`model-router.mjs`、`onebot-link.mjs`、`diagnostics/`。

- [ ] P2-01 明确消息、对话线程与业务任务的生命周期；为处理中的对话保存修订号与隐私代次，优先复用已有上下文状态。
- [ ] P2-02 按真实身份、引用与近期主题关联对话；群内多线程隔离，同名、@别人和@机器人分别处理。
- [ ] P2-03 保留确定性命令通道；普通聊天以当前输入为主，缺必要参数时追问。主动插话允许安静结束，不能要求所有消息都调用一次意图模型。
- [ ] P2-04 重排提示词为稳定规则与动态资料，固定工具声明顺序；随机风格后置，聊天/插话/日报/识图模板独立且共享短身份规则。
- [x] P2-05 移除“模型无正文 -> 随机应付短句”的失败路径。主动沉默不回退；真实错误按既有任务路由尝试备用，并再次核对权限与隐私代次。
- [ ] P2-06 对耗时业务复用任务执行器，缩短消息队列被长任务占用的时间；保持会话内必要顺序、有界队列和唯一发送者，禁止无界并行。
- [ ] P2-07 对话上下文只把已确认发送记为自己的成功发言；分段部分成功与结果未知保留，不把未送达正文当作用户看过。
- [ ] P2-08 增加提示词版本、脱敏输入组成与来源原因的对照，避免记录私有推理或默认持久化完整 prompt。

验收：@对象、引用接续、同名、多话题和纠正案例全部有测试；取消/超时/新消息到来时旧任务不能重复发送或覆盖新状态。需要说明是否仍有上下文不足的案例，不能用单元测试数量替代语义评价。

前端：诊断展示对话目标、选用来源、输入预算和结果类型；用户看见真实的等待/失败，不展示隐藏思考过程。回滚：用单一开关或版本切换恢复旧入口，灰度比较路径不得额外发送或写入两套对话状态。

#### P2 首批边界保护（1.4.16）

`cognition/chat-run.mjs` 使用进程内有界活跃表和 AsyncLocalStorage 将普通聊天作用域贯穿异步调用，不另建事件总线或持久化任务队列。新并发生成只替换相同用户/会话的旧生成；记录修订号、隐私代次与单调时限，finally 释放活跃表。关闭服务拒绝新运行并中止当前模型 HTTP 等待。

`memory-profile/generation.mjs` 复用已有用户代次，增加内存隐私代次；全体参与者旧上下文在任意记忆清理后保守失效。显式偏好只更新相应用户代次，不清空记忆；模型回复后的检查、传输重试、搜索和视觉轮次、逐段发送及记忆/图片缓存/表情后续效果均验证当前运行。发送已发生时不能撤回，未知回执仍保留为未知，不自动补发。匿名用量保留真实成本但不在遗忘后重新关联用户。

前端仅增加修订、代次和固定原因，不记录聊天正文或完整 prompt。管理员/JM 命令仍走原确定性入口，JM 私聊不受普通 AI 私聊门禁影响。

边界：当前保护的是已开始执行的聊天；WebSocket 的同群队列尚未改变，排队消息不提前做意图识别或抢占。180 秒是后续检查点期限，单个进行中请求仍受其原 HTTP 超时限制；已传给供应商的内容不承诺撤回。普通聊天取消和结果契约已实现，完整对话状态、工具总预算、队列并发及提示词质量仍需后续工作，P2 总目标不提前完成。

候选验收：`npm ci`、lint 0/0，本地 946 通过和 2 项 Windows 文件软链接权限跳过，Linux 948/948、13/13 离线回放、启动鉴权、JM 依赖和发布检查通过。前端以合成记录和真实管理 API 验证 1440/390 像素筛选及状态。真实配置只读挂载验证旧生成不会触发备用，FS 合成加密 ZIP 验证通过，无真实模型调用或 QQ 测试发送。运行中的模型请求仍可能产生费用；取消不会编造已撤回或零费用。

上线记录（2026-09-23）：代码提交 `e909830` 通过 GitHub CI 并部署 Linux `1.4.16-chat-lifecycle`；切换前受限状态备份完成，旧 1.4.15 发布目录与镜像保留。Bridge 已重新拉起且健康/就绪 200、QQ 已登录，JM 依赖与私聊帮助正常，新诊断静态资源已生效。配置、模型主备/思考档位和原归档校验和不变；日报计划一份，NapCat 未重启，Windows 未更新。

#### P2 提示词与完整轮次（1.4.17）

`system-prompts/compose.mjs` 为群聊/私聊/插话共享装配：固定 system 规则在前，权限内自身事实随后，再加入仅作表达参考的动态 user 层，最后保留所选上下文和当前输入。语气规则仍保留猫娘特征，明确表达设置不改变事实/权限。工具轮次复用该动态层，不重复附加；模型路由和思考档位不变。

`cognition/thread-manager.mjs` 继续使用原有有界存储，长摘录改保留首尾，新增截短标记；`context/budget.mjs` 对带 `contextAtomic` 的完整轮次只整条保留或舍弃，优先最近轮次，避免只有助手建议没有用户反馈。旧格式仍可读取；标记“已发送”不代表建议已执行或问题已解决，不新增自动事实推断写入。

实际调用诊断携带 `chat-v2` / `interjection-v2`、固定 system 的 SHA256 短指纹、固定与动态表达长度、组装文本长度。只记录元数据，不记录完整 prompt；文本计数不含图片或私有推理，也不等同供应商 token。整体请求预算、其他任务模板和缓存对照仍须后续完成，不能凭前缀稳定就宣称已省费。

候选验收：本地安装、lint 0/0、957 通过和 2 项 Windows 平台跳过；最终 Linux 959/959、13/13 离线回放、启动鉴权与发布检查通过，JM 依赖与实际 FS 合成加密包通过。实际协议模拟覆盖搜索工具轮次不重复动态层、保持固定前缀及只在内部保留工具往返所需推理字段；普通“补充上下文以便判断”的追问不再误判为内部规划，真正的自我计划和非正文推理仍被拒绝。

真实质量证据：9 次有界合成请求，不含生产聊天、QQ 发送、自动预热或额外意图调用。先发现问题再复测，短句承接从重复密码清单改为询问格式/解压软件，未知梗不编出处、不暗示已经查询；一次承接仍问了两项相关信息，不能称为严格单问题策略已经保证。其他三个场景仅有首候选单次样例证据，最终全场景回归留待 P5。实际合计输入 6347、缓存输入 2368、输出 1257（含推理697）token；这包含不同候选与失败尝试，不用于宣称缓存收益。模板、对话及缓存目标仍未全部完成。

上线记录（2026-09-23）：代码提交 `716c82f` 已通过 GitHub CI 并部署 `1.4.17-prompt-layers`。切换前完成受限状态备份，旧 1.4.16 源码与镜像保留；Bridge 重新拉起后健康/就绪 200、QQ 已登录，JM 与新诊断资源正常。原配置与归档校验和不变、日报计划一份，NapCat 未重启，Windows 未更新。本次不把 P2/P4 或最终 2.0.0 标为全部完成。

#### P2 发送状态批次（1.4.18）

`onebot-receipt.mjs` 统一成功/失败/未知判定，覆盖聊天、日报恢复判断及 JM 上传。异步受理、矛盾字段和 HTTP 异常不是成功，也不自动重发。原确定拒绝的有界重试仍保留；不改模型任务路线。

`cognition/delivery-ledger.mjs` 复用原子 JSON 持久化，先保存编号事件和发送尝试，再执行外发，记录确认/未知计数。事件键使用加盐 HMAC，不保存正文或明文身份；文件上限 10000 条，已结束约 24 小时清理，未核实不自动过期。单 Bridge 写入，损坏不重置。普通聊天的重播在观察、生成前停止；无消息编号的兼容调用不具备跨重启保证。

Linux 诊断可核实未知/部分/中断，核实不发送、不写成功聊天、不清事件门。遗忘解除身份索引、保留负向事件标记；时间边界复用既有持久化隐私账本。帮助/JM 等确定性命令仍走原权限，聊天状态异常不阻断它们。没有为业务命令承诺持久化幂等或自动恢复，P2-06 继续待实现。

本批推进 P2-01/P2-07，尚不勾选整阶段。新状态上线后不能无条件启动不认识这份账本的旧版本；首次部署回滚需检查新事件，否则保留状态并停 Bridge 待修，禁止回灌旧数据。验收和上线记录在实际完成后追加，最终 `2.0.0` 目标不变。

本地已完成隔离浏览器 1440/390 像素测试：实际管理 API 筛选、取消核实、保存后刷新及无横向溢出；没有模型调用或 QQ 发送。合成近满容量 9995 条、约 4.23 MB 的本地 Windows 状态，四次持久化的完整生命周期耗时约 109-158 ms；这是同步 JSON 的已知上限成本，不冒称 Linux 性能结论。后续候选需实际观察，不能为了性能省略发送前落盘。

本地安装与 lint 0/0，990 项测试中 988 通过、2 项 Windows 软链接权限跳过；Linux 候选 990/990 无跳过、13/13 离线回放、隔离启动鉴权、依赖和完整发布检查通过。实际配置只读挂载验证路线与 JM，真实 FS 合成加密包通过。SSH 已恢复，候选验证完成；GitHub CI、备份切换与线上状态另行记录，不能把本批当作整个目标完成。

调用方交叉审计补充：普通资源转发原 `status=ok OR retcode=0` 会误认矛盾回执，命令分发原来无条件写助手标记，登录/引用/成员/表情读取也有宽松分支，均改为共享判定。子进程退出码不属于 OneBot 协议，没有机械替换。词云返回值仍是“命令已处理”，不等同“已发送”；业务任务的全量明确结果契约及重启幂等仍待后续批次，P1-04/P2-06 不据此勾选。

上游 [NapCat v4.18.13 UploadFileStream](https://github.com/NapNeko/NapCatQQ/blob/v4.18.13/packages/napcat-onebot/action/stream/UploadFileStream.ts) 最终按 filename 写入公共临时目录，同名文件存在覆盖和旧清理计时器影响新文件的风险。本批对远端临时名加随机编号，保留 QQ 展示名；回执提供的流编号、大小和校验和需匹配。[上游测试](https://github.com/NapNeko/NapCatQQ/blob/v4.18.13/packages/napcat-onebot/action/stream/test_upload_stream.py) 确认成功外层为 OneBot 响应，内部完成态为 file_complete。Linux 候选已进行两份同名微型合成文件的真实流式传输验证，远端路径各自独立；仅写 NapCat 临时目录约 60 秒，不发 QQ 消息、不下载漫画，不重启 NapCat。

上线记录（2026-09-23）：代码提交 `5481c8a` 已通过 GitHub CI 并部署 `1.4.18-delivery-state`，受限备份目录 0700、归档 0600；Bridge 重建后健康/就绪 200、QQ 登录与 JM 检查通过，新诊断可读、发送目录可写。私有配置和旧归档校验和不变，日报计划一份，NapCat 未重启、Windows 未更新。当前规则只覆盖新版实际接纳的事件，不回填旧版发送确认。隔离目录与生产数据同盘的 Linux 合成近满容量测试（9995 条、约 4.23 MB）四次持久化耗时 89–136ms；未读写真实聊天，不等同端到端或长期并发性能保证。

#### P2/P3 引用来源批次（1.4.19）

`context/quoted-reply.mjs` 验证传输层返回的最小来源数据；`napcat.mjs` 仍负责 OneBot 读取，不在传输层写记忆。当前群、引用消息编号、作者与发送时间缺一不可；本次来源校验不等于内容真实性验证，也不从昵称猜身份。遗忘读取既有 `summaryPrivacy` 截止线，不新增第二套隐私数据库。

入口捕获的 `contextPrivacyGeneration` 跨过群成员/引用读取、私聊预处理和聊天运行的建立时刻。读取期间或建立运行前发生清理，旧输入不能继续观察、调用模型或回填；私聊文件最后一次 await 后同样检查。画像清理只保持原画像含义，完整“忘记我”才使用持久化用户截止线。

`context/messages.mjs` 和原预算模块共同构造引用层：稳定作者 ID、消息编号、时间、受限首尾摘录，整体进入或退出预算。取不到原话时添加缺失提示，抑制借用近期群聊、个人历史或旧线程来替代该引用；没有宣称永不需要用户补充。

`contextPacket.currentInput` 作为内部输入快照流向原主/备模型入口，避免预算按一份输入计算、实际调用又重建另一份。普通/私聊/文件/插话与 DeepSeek 备用覆盖，特殊搜索摘要模板仍在后续 P3/P4 统一范围内。附件内容不触发隐式预搜索；普通聊天搜索与任务主备不变。不新增每轮意图模型，不新增持久化记忆格式。

前端只投影核验状态、作者 ID、时间和原因；原始引用和图片链接不进诊断记录。chat-v3 / interjection-v3 明确接收人与所问对象的区别，允许正常不确定表达，输出清洗与推理字段隔离不放松。作者字段冲突直接拒绝，顶层/嵌套单一作者及数字/字符串等价形式保持兼容。P1/P2/P3 全阶段不能因本批而勾选完成。

候选验收：Linux 1011/1011 无跳过，本地 1009 通过与 2 项 Windows 软链接权限跳过；lint 0/0、13/13 回放、npm ci/依赖/完整发布检查及隔离启动鉴权通过。JM 实际依赖和 FS 合成 ZIP 可用，不发 QQ 消息。浏览器使用实际管理 API 与合成记录在 1440/390 像素检查来源筛选、脱敏和布局。独立审查发现的作者冲突已补测试并复核。

有界语义检查实际调用 4 次，覆盖同名引用未执行的计划、缺失引用各两个主备插槽，沿用线上思考档位。两模型均未错归说话人、未把计划当完成，缺失时自然请求原话，DeepSeek 追问仍略长。仅这两个虚构样例通过，不推论全局质量、真实图片识别或缓存节省；未读生产聊天、未存推理正文。

上线记录（2026-09-23）：代码 `440f8f5` 已通过 GitHub CI 并部署 Linux `1.4.19-context-provenance`。切换前私有备份目录 0700、归档 0600，gzip 完整性通过；最终可执行源码与已验收候选比对一致。Bridge 重建后健康/就绪 200、QQ 登录、JM、发送账本和诊断资源通过；模型配置、sidecar 和旧归档哈希不变，日报计划一份，NapCat 未重启，Windows 未更新。前版 1.4.18 能读取同一发送账本，回滚仅切代码/镜像并保留新数据；回滚会失去本批引用保护，不能称作同等能力。

### F. P3 记忆、工具与图片

入口：`bridge/memory-profile/`、`context-retriever.mjs`、`context/`、`vision.mjs`、`vision-provider.mjs`、`system-prompts/image-context.mjs`、`model-mimo.mjs`、`api-providers/request-privacy.mjs`。

- [ ] P3-01 将显式偏好、事实事件、待办/当前状态和模型推测分开表达，补来源与失效字段；旧数据兼容读取，不伪造缺失证据。
- [ ] P3-02 新纠正使旧结论被替代；遗忘清理关联记忆、派生缓存并增加代次。正在运行的画像、模型和工具结果返回时再次检查，防止旧信息回填。
- [x] P3-03 在已有检索上提供受限只读工具，后端绑定当前用户/群和时间范围，限制结果数量。没有命中时不能自动跨群或放宽权限。（1.4.21，Linux 验收、隔离/取消测试及主备真实合成往返通过。）
- [x] P3-04 提供简短能力快照和按需状态查询；允许模型报告未知或失败，不提供服务器任意文件读取和系统命令能力。（1.4.21，配置/缓存与未知分开，实际模型身份随插槽。）
- [ ] P3-05 为工具轮次、时间、输入输出和总用量设有界预算；明确哪些命令可转成模型工具，管理员命令和有外发副作用的操作默认不开放。
- [ ] P3-06 视觉输入接入同一条对话选择结果。在配置能力允许时提供原图和相关原话，否则使用已有视觉描述路径；不把客观画面和语境推断混为事实。
- [ ] P3-07 旧上下文超预算时按完整轮次处理，保留引用、工具调用/结果配对、纠正和来源；付费压缩需有阈值和用量上限，普通聊天不默认多一次总结。
- [ ] P3-08 现有关系计算继续使用原算法和合法来源；本轮不自动把新事实类型变成关系加分项。

验收：权限拒绝、无命中、工具超时和达到预算时正常结束；同一图片在不同上下文不能复用错误语境解释。显式称呼变化立即进入下一次对话；遗忘后旧异步结果和缓存不能复活记忆。私聊不因新模块获得默认长期存储。

前端：管理员在授权范围内查看来源、修改/停用错误记录；区分人工确认与模型推测，编辑有版本冲突检查。回滚：新字段向后兼容，明确新写入数据怎样保留，不通过还原旧备份抹掉上线后的记录。

#### P3 明确记忆批次（1.4.20）

`memory-profile/notes.mjs` 在原画像 JSON 中增加有界明确条目，不复制全部聊天，也不增加每轮提取模型。本人命令、自述来源和管理员备注分开，只有带真实事件消息编号的命令能标为用户自述；管理员请求不能伪造来源。条目有固定期限、单条修订和整体快照冲突检查，纠正替换正文、仅保留有界旧来源编号。

`memory-profile/evidence.mjs` 复用原群历史，近期话题线索要求本群、编号、时间和去重证据，只表示谈过，不推断喜恶/经历。聊天按相关性注入明确条目，旧规则画像不再注入普通聊天或被提及者提示；关系模块原有字段、置信度和分数计算仍兼容，不把新记忆加进关系分。

纠正会让关联旧来源与纠正前相关线程退出本轮上下文；既有隐私代次使在途生成失效，完整遗忘清理全部范围条目，并用原有持久化截止线防止旧快照回写。私聊不自动增加长期记录，只有显式记忆命令保存单条。命令/前端保存必须确认既有 saver 落盘，存储失败不报成功、不重置坏文件。

现有 Linux 浏览器新增按范围和用户的记忆页，支持新建/修改/删除、冲突、未保存草稿、来源和有效期；前端只读显示原明确偏好和有来源的主题线索。帮助目录从现有 manifest/capability 入口同步，不新增第二套命令注册表。用户说明见 [明确记忆](MEMORY.md)。

独立审查补齐入口前代次、命令等待后权限、发送各段/重试前的检查。聊天线程记录实际入选记忆的编号和修订，不再靠词面能否召回来判断旧依赖是否过时；无依赖元数据的旧线程保守失效。来源否定标记与条目展示的短历史分开，至少保留一天且原话仍在滚动历史时不移除，全局上限 8192；达到上限拒绝覆盖，不把旧声明重新当有效来源。当前群不同发言人和插话同样排除已替代来源，但不因此获取他人的备注。

最终候选通过 npm ci/audit0、lint0/0、Linux1069通过+1项独立浏览器检查、本地1067通过+2项Windows软链接跳过+1项独立浏览器检查；浏览器14/14及实际管理API的1440/390像素CRUD/冲突/私聊隔离通过，13/13回放、隔离启动鉴权、依赖和完整发布检查通过。真实JM依赖/FS合成包通过；没有漫画下载或QQ测试发送。新增59项测试，未减少原测试。

真实合成模型请求合计6次，不读生产聊天、不记录推理正文，主备及思考档位未改。前候选的纠正系统信息样例主备均通过，但缺失记录样例存在自动长期记忆承诺；最终候选加入实际选用条目数量/读取状态与chat-v4、interjection-v4，2次复查改为范围内缺资料的追问。MiMo仍略有技术措辞，前候选的纠正样例未再次收费复跑；P5需最终全场景回归，不能把混合候选结果包装为全部质量达标或缓存收益。

本批推进 P3-01/P3-02，但并非完成所有事件/待办状态建模、模型只读工具、图片及总预算。最终 2.0.0 仍不提前发布，上线后再补真实部署记录。

上线记录（2026-09-23）：代码 `f0cba00` 经 GitHub CI 和第二候选验收后部署 `1.4.20-memory-evidence`。先确认服务器原画像集合结构兼容且无旧 notes 字段，不打印内容；受限状态备份 0700/0600、gzip 完整性通过。Bridge 重建后健康/就绪 200、QQ 登录、JM、发送账本和新记忆静态资源正常，接口无范围读取返回 400。配置和归档哈希不变，日报计划一份，NapCat 未重启，Windows 未更新。原 1.4.19 代码/镜像保留，回滚不覆盖新状态；旧版不会使用新记忆功能，未来恢复新版仍须保留原隐私截止线和来源否定标记。

#### P3 受限工具批次（1.4.21）

新增 `chat-tools/` 单一执行入口：`policy` 固定工具声明与范围，`session` 维护本轮预算与隐私代次，`read` 读取既有记忆/权限状态，`runner` 组织模型往返。主/备及私聊/文件聊天共用执行器，替代 MiMo 旧工具循环和 DeepSeek 隐式预搜索；不增加通用插件框架、工具数据库或管理员执行权。

`recall_memory` 只读本人当前群的带来源条目/原话，私聊只读明确记忆；`read_bot_status` 只读权限内配置和缓存，不探测 API；`web_search` 仅接受当前消息肯定查询片段，不从隐藏记忆拼查询。工具不能写文件、调用系统命令、改变配置或发送 QQ 消息。模型决定是否查，后端决定是否准许以及允许读哪里。

所有回合共享 4 次逻辑模型调用、8 次 HTTP 尝试、4 次工具处理和 90 秒期限，主插槽给备用留一轮。输入 24000 字符、工具单次/累计 2000/6000 字符、接收 256 KiB、正文 6000 字符都有硬边界；拒绝回执也计入预算。文件、插话不允许外部搜索。隐私、权限变化后旧调用结果和本轮缓存失效。

Responses/Anthropic 同插槽保留原生协议次序和签名；备用只拿完整只读资料，不拿主模型的私有推理和模型身份。Gemini 和无工具兼容端点不伪称支持原生记忆工具。记忆依赖修订跟随实际工具结果进入已发送线程，纠正/删除后旧答案上下文失效。诊断页只显示固定工具名、状态、回合与预算，不展示正文或原生续接字段。

本批推进 P3-03/P3-04/P3-05，尚未完成更广事件/待办状态、统一图片上下文、全部任务用量预算或语义召回。规则召回可能对仅含旧词的纠正追问返回未命中，不将撤回内容重新返回。详见 [聊天只读工具](CHAT-TOOLS.md)。不提前声明全 P3 或 2.0.0 完成。

候选验收（2026-09-23）：npm ci/audit0、lint0/0、Linux 1135 通过/1 项可选浏览器跳过；同源本地 1133 通过/2 项 Windows 软链接跳过/1 项浏览器跳过，浏览器专项另跑 14/14。真实管理 API 的诊断页面 1440/390 像素截图与字段隐私检查通过，13/13 回放、隔离启动鉴权、完整 Linux 发布门禁、451 文件发布包扫描通过。真实 JM 依赖和大写 FS 合成 ZIP 通过，无漫画下载及 QQ 测试发送。新增 66 项测试，原测试未删除。

独立审查复现并修复了否定搜索、超大模型返回、错误回执挤超预算；再核对 14 个否定和 6 个肯定搜索样例。最终候选真实接口只调用 4 次：MiMo 与 DeepSeek 各 2 回合，均查询本群合成记忆与本轮模型状态、返回 Debian 容器和正确模型标识，并记录所用条目修订。供应商报告的主/备输入为 4106/4416 token，缓存输入 1792/2304，输出 121/186；这只是两条带工具的合成样例，不是一般缓存节费证明，也不代表全部协议和语义场景已实测。

上线记录（2026-09-23）：代码 `246cf16` 通过 [GitHub Linux CI](https://github.com/awaa4962-coder/qqbot-core/actions/runs/35837657422)，最终执行源码与候选一致，部署 `1.4.21-scoped-tools`。切换前受限状态备份目录 0700、归档 0600、gzip 完整性通过。Bridge 正常退出后重建，健康/就绪 200、QQ 登录、JM、记忆和诊断静态资源均正常；API 配置、私有 sidecar、退役梗库归档哈希不变，日报计划一份。NapCat 未重启，Windows 未更新。原 1.4.20 代码/镜像保留，回滚仅切代码和镜像、不恢复旧数据；回滚会失去本批工具限制与功能，不能称作同等能力。P3-05 的聊天工具预算已实现，但视觉预处理和其他模型任务的整体验证仍需后续收口。

#### P3 图片语境批次（1.4.22）

`vision/images.mjs` 负责安全下载、真实图片格式/像素/字节校验和内存 JPEG 规范化；`vision/session.mjs` 为主备绑定同一已选图片集合；`vision.mjs` 只做必要的客观描述，`vision/description-cache.mjs` 保存短时同范围客观描述。没有新增全套视觉代理或重复图库，已有表情指纹用途保留。

聊天插槽的实际配置支持视觉时原像素与已选对话一起进入请求；文字插槽才走视觉任务主备，原图只下载一次。当前、核验引用、明确指向的近期图片有来源标签；近期原消息进入原有上下文预算，过期/遗忘/撤回/纠正来源不回填。动态图只读首帧，坏图/超限有明确计数，禁止模型补写未看到的内容。

图片字节预算与 24000 字符文字/协议预算分开，但只有本轮注册的规范化 JPEG 能豁免文字计数；不合法角色、伪装内容类型、重复超量和请求覆写均拒绝。签名和私有续接仍完整计量，备用不携带前槽推理。客观描述缓存按精确内容哈希、用户/会话、提供方配置、提示词与图片布局隔离，绝对十分钟/128条/128KiB，只存内存；遗忘同步清空，在途重新核对。

独立审查发现并修复：资源自身超时误当整轮取消、接口错误内容回显图片/URL、最终正文和客观描述接受图片编码。资源超时降级，不吞调用方取消；传输仅报告 HTTP 与固定错误类别，输出拒绝图片 URI、已知图片编码和与本轮输入相同的长编码片段。诊断只显示路径/数量，明确像素不能用文字脱敏规则抹去隐私。详见 [图片与对话](VISION.md)。

最终第三候选：本地1232通过/2项Windows软链接跳过/1项可选浏览器跳过；Linux1234通过/1项可选浏览器跳过，总计1235项，较上批新增99项。浏览器专项独立14/14，实际管理API的1440/390看图路径及计数截图通过；lint0/0、npm ci/审计0、13/13回放、隔离启动鉴权、完整Linux发布门禁和460文件发布包检查通过。真实JM依赖及大写FS合成ZIP验证通过，未发送QQ或下载漫画。辅助验收脚本曾硬编码过时的sharp内部路径，改用包入口后实际候选通过，生产模块不受影响。

真实模型质量累计12次请求，三轮各4次，仅同一合成GOOD JOB图片和两种明确语境，没有生产聊天或真实群图。第一候选MiMo反话回复冗长且猜动机，第二候选把反话偏解成打气；最终chat-v8先核对字面与前文事实冲突，主备均识别两种语境，但MiMo反话仍附加“安慰/打气”的推测。最终四条中该条未达到预期的动机边界，留在P5缺陷清单，不再反复付费试跑或宣称全部合格。最终4次均走实际配置允许的原图直传，未发生视觉描述兜底；描述兜底/缓存/原生多协议由合成协议和真实Linux依赖测试覆盖，不能冒充这些端点都真实计费跑过。

最终四次供应商报告输入总计7738、缓存输入1664、输出277token；这不是冷/热受控比较，不用于宣称节费。描述缓存只缓存客观材料，不缓存同图语境答案。P3-06工程链路和P4-03缓存边界已实现，仍须在上线核验后记录状态；图片识别语义、其他任务总预算和整体验收仍未完成。

### G. P4 缓存与成本

入口：`bridge/system-prompts/`、`model-mimo.mjs`、`model-ds.mjs`、`api-providers/usage-metrics.mjs`、视觉与检索所属模块。

- [ ] P4-01 固定相同任务的静态模板、工具 schema 和序列化顺序；动态数据后置，提示词指纹在请求外计算，不把随机标识塞入稳定前缀。
- [ ] P4-02 在现有用量记录中增加必要模型/提示词版本维度，按任务、主备和模式区分。缺失缓存字段显示未知，不伪报为零或命中。
- [ ] P4-03 图片客观识别缓存以内容哈希、模型、提示词版本和访问范围为键；语境解释另外考虑对话修订，必要时不缓存。
- [ ] P4-04 搜索/检索仅做有容量与 TTL 的必要缓存；记忆修改、遗忘、权限变动和模型/模板变化使相关结果失效。
- [ ] P4-05 仅在同一授权范围与输入版本内合并相同的并发只读计算，独立处理等待者取消；不合并发送，不缓存失败为成功。
- [ ] P4-06 普通聊天最终正文不做语义相似答案缓存；帮助/版本等可确定内容继续本地生成，不为它们调用模型。
- [ ] P4-07 对比冷/热请求的实际缓存 Token、耗时与输入/输出/思考用量，不用平均百分比冒充 Token 加权命中率。

验收：跨用户/群隔离、修订失效、TTL/容量淘汰、错误恢复和并发取消有测试。缓存只减少可复用计算，不能改变事实、权限或任务结果。供应商缓存是尽力机制，不承诺 100% 命中，也不承诺本地遗忘能控制供应商内部缓存删除。

前端：保留成员查看自己缓存统计的能力；管理员查看按任务/模型/提示词版本的对照。明确 API 前缀缓存与本地缓存的区别，不用命中率单独代表回答质量或总费用。

### H. P5 整合、上线与清理

- [ ] P5-01 各阶段前端功能整合，验证桌面/手机布局、无权限、空数据、长任务、保存失败、版本冲突、刷新恢复和取消后的真实状态。
- [ ] P5-02 复跑固定案例，记录内容质量变化及实际用量；只改提示词的候选也必须经过纠正、权限和发送边界测试。
- [ ] P5-03 列出剩余旧接口与调用方，移除已无消费者的重复实现，保留确有需要的薄适配层和兼容测试。
- [ ] P5-04 完成 Linux CI、候选镜像、源码/私有状态备份和恢复检查；明确选定灰度群，不默认向所有群发布测试回复。
- [ ] P5-05 先单群运行，再根据预先确定的样例覆盖与观察窗口扩大；线上出现隐私、错误收件人、重复发送、推理外发立即停新路径。
- [ ] P5-06 核验服务器版本、`/health`、`/ready`、NapCat 连接与定时任务唯一性；完成更新日志、帮助、工作流和 GitHub 同步。
- [ ] P5-07 按用户指定，本轮全部改造和验收完成后统一正式版本为 `2.0.0`。同步 `package.json`、锁文件根版本、`bridge/version.mjs`、版本/更新命令、中英文说明、前端与 Linux 镜像/运行版本，GitHub 记录一致；不能把阶段性版本提前标为最终完成。

## 实施验证清单

下列命令用于代码批次的基本门禁；Windows 本地使用 `npm.cmd`，Linux 使用 `npm`。实际运行环境变更才启动隔离候选，不为纯文档改动启动 Bot。

```bash
npm ci
npm run lint
npm test
npm run replay:check
npm run release:check
```

Linux CI 的依赖审计与运行检查也必须通过。已有测试数量不减少，安全与隐私断言不删，不关闭 ESLint。Windows 跳过的平台专项测试必须由 Linux 验证，不能把跳过计为通过。

| 风险面 | 必测内容 |
| --- | --- |
| 兼容 | 群/私聊命令、管理员独立权限、功能白名单、日报去重、JM 与表情不受梗库退役影响 |
| 状态 | 无正文、主动沉默、真实失败、备用成功、部分发送、未知回执、重启中断 |
| 记忆 | 证据范围、复读、纠正、到期、人工修改、遗忘代次、异步旧结果拒绝 |
| 模型 | 现有主备配置不变、多协议工具往返、私有推理不外发、不记录 key |
| 缓存 | 正确键与隔离、过期、数据/模型/模板更新、并发合并、不缓存外发动作 |
| 前端 | 操作反馈、权限裁剪、任务恢复、编辑冲突、错误可见、无假完成 |
| 质量 | 回答对象、贴题程度、事实依据、图片语境、不必要插话和自然表达 |

## 每批次交付记录

执行时为实际批次填写，未做的检查标明“未执行”与原因，不能预填完成。

```text
阶段 / 任务 ID：
实际修改模块与唯一入口：
兼容层新增 / 删除及剩余调用方：
数据格式 / 配置 / 权限变化：
备份与恢复核验：
lint / test / replay / release 检查：
实际模型调用次数与质量评价：
前端验证：
GitHub 分支 / commit / CI：
Linux 候选 / 已部署版本 / 健康检查：
灰度范围与结果：
剩余问题 / 回滚条件：
```

当前 P0 已完成，P1 能力状态和自动梗库退役已上线，其余 P1 与 P2-P5 仍需继续实施；不得把单个批次的通过表述为智能化改造全部完成。
